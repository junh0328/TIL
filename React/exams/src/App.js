import "./App.css";
import StyledComponents from "./components/StyledComponents";

function App() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}
    >
      <StyledComponents />
    </div>
  );
}

export default App;
